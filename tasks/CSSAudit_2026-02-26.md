# CSS Audit - style.css

**Date:** 2026-02-26
**File:** style.css
**Rules evaluated:** 40 across 11 families

---

## Summary

| Family | Rules | PASS | FAIL | N/A |
|---|---|---|---|---|
| NAME | 5 | 2 | 3 | 0 |
| FMT | 4 | 1 | 3 | 0 |
| PROP | 2 | 1 | 1 | 0 |
| DGR | 4 | 1 | 3 | 0 |
| RESP | 3 | 0 | 3 | 0 |
| BRK | 4 | 1 | 2 | 1 |
| OVF | 3 | 3 | 0 | 0 |
| LAY | 5 | 0 | 5 | 0 |
| ANIM | 3 | 0 | 2 | 1 |
| ACC | 5 | 2 | 1 | 2 |
| ST-REUSE | 2 | 1 | 1 | 0 |
| **Total** | **40** | **12** | **24** | **4** |

### FAILs by priority

| Priority | Count |
|---|---|
| High | 8 |
| Medium | 8 |
| Low | 8 |

---

## NAME - Naming

| ID | Status | Priority | Notes |
|---|---|---|---|
| NAME-01 | FAIL | Low | See details below |
| NAME-02 | FAIL | Low | See details below |
| NAME-03 | FAIL | Low | See details below |
| NAME-04 | PASS | - | - |
| NAME-05 | PASS | - | Uses ST/core IDs only; no extension-created ID selectors found |

### NAME-01 - FAIL (Low)

**Violation** (line 1403):
```css
.stwid-compactInput {
```

**Corrected:**
```css
.stwid--compact-input {
```

### NAME-02 - FAIL (Low)

**Violation** (line 155):
```css
.stwid--thinContainer {
```

**Corrected:**
```css
.stwid--thin-container {
```

### NAME-02 - FAIL (Low) - violation 2

**Violation** (line 1307):
```css
.stwid--RowTitle {
```

**Corrected:**
```css
.stwid--row-title {
```

### NAME-03 - FAIL (Low)

**Violation** (line 488):
```css
.stwid--small-multiselect {
```

**Corrected:**
```css
.stwid--visibility-filter-select {
```

---

## FMT - Formatting

| ID | Status | Priority | Notes |
|---|---|---|---|
| FMT-01 | FAIL | Low | See details below |
| FMT-02 | FAIL | Low | See details below |
| FMT-03 | PASS | - | - |
| FMT-04 | FAIL | Low | See details below |

### FMT-01 - FAIL (Low)

**Violation** (line 510):
```css
.stwid--small-multiselect a[data-selected="true"] {
```

**Corrected:**
```css
.stwid--small-multiselect a[data-selected='true'] {
```

### FMT-01 - FAIL (Low) - violation 2

**Violation** (line 1411):
```css
.stwid--orderHelper :is(.stwid--thinContainer, .stwid--bulkEditContainer)[data-field="outlet"] .stwid--input {
```

**Corrected:**
```css
.stwid--orderHelper :is(.stwid--thinContainer, .stwid--bulkEditContainer)[data-field='outlet'] .stwid--input {
```

### FMT-02 - FAIL (Low)

**Violation** (line 97 onward): content inside `#WorldInfo { ... }` is not consistently indented to reflect nesting.

**Corrected:**
```css
#WorldInfo {
  /* 1.5) Shared layout shells */
  .stwid--editor {
    flex: 1 1 auto;
  }
}
```

### FMT-04 - FAIL (Low)

**Violation** (lines 1719, 1731, 1738, 1767): repeated parent prefix `.stwid--orderTable tr td` appears in 4+ separate blocks without nesting.

**Corrected:**
```css
.stwid--orderTable tr td {
  .stwid--sortableHandle {
    cursor: pointer;
  }

  .stwid--orderMove {
    display: flex;
  }

  .stwid--orderMoveButton {
    display: inline-flex;
  }

  .stwid--orderSelect {
    display: flex;
  }
}
```

---

## PROP - Properties

| ID | Status | Priority | Notes |
|---|---|---|---|
| PROP-01 | FAIL | Low | See details below |
| PROP-02 | PASS | - | - |

### PROP-01 - FAIL (Low)

**Violation** (lines 1405-1406):
```css
padding-left: 5px;
padding-right: 5px;
```

**Corrected:**
```css
padding: 0 5px;
```

---

## DGR - Danger

| ID | Status | Priority | Notes |
|---|---|---|---|
| DGR-01 | FAIL | High | See details below |
| DGR-02 | PASS | - | - |
| DGR-03 | FAIL | Low | See details below |
| DGR-04 | FAIL | Medium | See details below |

### DGR-01 - FAIL (High)

**Violation** (line 60):
```css
display: flex !important;
```

**Corrected:**
```css
/* override: ensure core openDrawer state always renders the extension drawer */
display: flex !important;
```

### DGR-01 - FAIL (High) - violation 2

**Violation** (line 420):
```css
opacity: 1 !important;
```

**Corrected:**
```css
/* override: ST dropdown utilities can force opacity; keep drawer menu visible when active */
opacity: 1 !important;
```

### DGR-01 - FAIL (High) - violation 3

**Violation** (line 953):
```css
flex: 1 1 auto !important;
```

**Corrected:**
```css
/* override: core WI content block sets conflicting flex values */
flex: 1 1 auto !important;
```

### DGR-03 - FAIL (Low)

**Violation** (line 176):
```css
i.fa-solid.fa-fw.fa-circle-question.stwid--thinContainerLabelHint {
```

**Corrected:**
```css
.fa-solid.fa-fw.fa-circle-question.stwid--thin-container-label-hint {
```

### DGR-03 - FAIL (Low) - violation 2

**Violation** (line 1807):
```css
a.stwid--comment.stwid--commentLink {
```

**Corrected:**
```css
.stwid--comment.stwid--comment-link {
```

### DGR-04 - FAIL (Medium)

**Violation** (line 1094):
```css
.stwid--blocker .stwid--listDropdownMenu .stwid--listDropdownItem #lorebook_ordering_button {
```

**Corrected:**
```css
.stwid--listDropdownItem #lorebook_ordering_button {
```

### DGR-04 - FAIL (Medium) - violation 2

**Violation** (line 1760):
```css
.stwid--orderTable tr td .stwid--sortableHandle:focus-visible {
```

**Corrected:**
```css
.stwid--orderTable .stwid--sortableHandle:focus-visible {
```

---

## RESP - Responsive Units

| ID | Status | Priority | Notes |
|---|---|---|---|
| RESP-01 | FAIL | Medium | See details below |
| RESP-02 | FAIL | Medium | See details below |
| RESP-03 | FAIL | High | See details below |

### RESP-01 - FAIL (Medium)

**Violation** (line 132):
```css
width: 300px;
```

**Corrected:**
```css
width: min(30vw, 22rem);
```

### RESP-02 - FAIL (Medium)

**Violation** (line 132): fixed pixel width on the list panel has no `max-width: 100%`.

**Corrected:**
```css
width: min(30vw, 22rem);
max-width: 100%;
```

### RESP-03 - FAIL (High)

**Violation** (line 132): major layout region (`.stwid--list`) uses fixed pixel width.

**Corrected:**
```css
.stwid--list {
  flex: 0 1 30%;
  width: min(30vw, 22rem);
  min-width: 0;
}
```

---

## BRK - Breakpoints

| ID | Status | Priority | Notes |
|---|---|---|---|
| BRK-01 | FAIL | Medium | See details below |
| BRK-02 | FAIL | High | See details below |
| BRK-03 | PASS | - | No `min-width` media queries found |
| BRK-04 | N/A | - | No `@media` blocks present to validate placement |

### BRK-01 - FAIL (Medium)

**Violation:** no `@media screen and (max-width: 1000px)` or `@media screen and (max-width: 768px)` blocks were found.

**Corrected:**
```css
@media screen and (max-width: 1000px) {
  .stwid--list,
  .stwid--editor {
    width: 100%;
    max-width: none;
  }
}

@media screen and (max-width: 768px) {
  .stwid--controls .stwid--controlsRow {
    gap: 0.5em;
  }
}
```

### BRK-02 - FAIL (High)

**Violation** (lines 69 and 127): major layout containers use side-by-side flex with no mobile override.

**Corrected:**
```css
@media screen and (max-width: 1000px) {
  body.stwid-- #WorldInfo > .stwid--body {
    flex-direction: column;
  }
}
```

---

## OVF - Overflow

| ID | Status | Priority | Notes |
|---|---|---|---|
| OVF-01 | PASS | - | Drawer root and body use `overflow: hidden` |
| OVF-02 | PASS | - | Wide table is wrapped (`.stwid--orderTableWrap { overflow: auto; }`) |
| OVF-03 | PASS | - | Constrained text rows use truncation or controlled layout |

---

## LAY - Layout

| ID | Status | Priority | Notes |
|---|---|---|---|
| LAY-01 | FAIL | High | See details below |
| LAY-02 | FAIL | High | See details below |
| LAY-03 | FAIL | Medium | See details below |
| LAY-04 | FAIL | Medium | See details below |
| LAY-05 | FAIL | Medium | See details below |

### LAY-01 - FAIL (High)

**Violation** (line 69):
```css
body.stwid-- #WorldInfo > .stwid--body {
  display: flex;
}
```

No `@media screen and (max-width: 1000px)` rule switches this layout to column.

**Corrected:**
```css
@media screen and (max-width: 1000px) {
  body.stwid-- #WorldInfo > .stwid--body {
    flex-direction: column;
  }
}
```

### LAY-02 - FAIL (High)

**Violation** (line 127):
```css
.stwid--list {
  width: 300px;
}
```

No mobile override sets list/editor to full width.

**Corrected:**
```css
@media screen and (max-width: 1000px) {
  .stwid--list,
  .stwid--editor {
    width: 100%;
    max-width: none;
  }
}
```

### LAY-03 - FAIL (Medium)

**Violation** (line 832): splitter is always visible.

**Corrected:**
```css
@media screen and (max-width: 1000px) {
  .stwid--splitter {
    display: none;
  }
}
```

### LAY-04 - FAIL (Medium)

**Violation** (line 205):
```css
.stwid--sortingRow {
  flex-wrap: nowrap;
}
```

No mobile override enables wrapping.

**Corrected:**
```css
@media screen and (max-width: 1000px) {
  .stwid--sortingRow,
  .stwid--visibilityRow {
    flex-wrap: wrap;
  }
}
```

### LAY-05 - FAIL (Medium)

**Violation:** no mobile rule ensures Order Helper panel full-width behavior and explicit table-wrapper scrolling contract.

**Corrected:**
```css
@media screen and (max-width: 1000px) {
  .stwid--orderHelper {
    width: 100%;
    max-width: none;
  }

  .stwid--orderTableWrap {
    display: block;
    overflow-x: auto;
  }
}
```

---

## ANIM - Animation

| ID | Status | Priority | Notes |
|---|---|---|---|
| ANIM-01 | FAIL | High | See details below |
| ANIM-02 | FAIL | Medium | See details below |
| ANIM-03 | N/A | - | No `will-change` usage found |

### ANIM-01 - FAIL (High)

**Violation** (line 1316):
```css
transition: margin-bottom 250ms ease;
```

**Corrected:**
```css
transition: transform 250ms ease;
```

### ANIM-01 - FAIL (High) - violation 2

**Violation** (line 1342):
```css
transition: max-height 250ms ease;
```

**Corrected:**
```css
transition: transform 250ms ease, opacity 250ms ease;
```

### ANIM-02 - FAIL (Medium)

**Violation** (line 647):
```css
transition: 200ms;
```

**Corrected:**
```css
transition: opacity 200ms ease;
```

### ANIM-02 - FAIL (Medium) - violation 2

**Violation** (line 1572):
```css
transition: 200ms;
```

**Corrected:**
```css
transition: background-color 200ms ease, opacity 200ms ease;
```

---

## ACC - Accessibility

| ID | Status | Priority | Notes |
|---|---|---|---|
| ACC-01 | PASS | - | Focus-visible styles are present for key interactive controls |
| ACC-02 | FAIL | High | See details below |
| ACC-03 | PASS | - | No clear case of keyboard-required controls hidden without replacement |
| ACC-04 | N/A | - | Icon-label check is markup-level, not CSS-only |
| ACC-05 | N/A | - | `tabindex` is markup-level, not CSS-only |

### ACC-02 - FAIL (High)

**Violation:** transitions are defined (for example lines 346, 437, 576, 1072, 1316, 1329, 1342) with no `prefers-reduced-motion` override in the file.

**Corrected:**
```css
@media (prefers-reduced-motion: reduce) {
  .stwid--iconTabButton,
  .stwid--folder .stwid--folderHeader,
  .stwid--rowContentWrap,
  .stwid--collapseChevron {
    transition: none;
  }
}
```

---

## ST-REUSE - SillyTavern Style Reuse

| ID | Status | Priority | Notes |
|---|---|---|---|
| ST-01 | PASS | - | ST classes are reused where relevant (`text_pole`, `menu_button`, `inline-drawer`) |
| ST-02 | FAIL | High | See details below |

### ST-02 - FAIL (High)

**Violation** (line 1430):
```css
color: var(--warning-color, #e8a97f);
```

**Corrected:**
```css
color: var(--warning-color, var(--SmartThemeQuoteColor));
```

### ST-02 - FAIL (High) - violation 2

**Violation** (line 1701):
```css
color: var(--SmartThemeErrorColor, #e57373);
```

**Corrected:**
```css
color: var(--SmartThemeErrorColor, var(--SmartThemeQuoteColor));
```

---

## Status icon key

| Symbol | Meaning |
|---|---|
| PASS | Rule satisfied |
| FAIL | Violation found - see details block below the table |
| N/A | Rule does not apply (construct absent from this file) |

## Priority key

| Level | When to use |
|---|---|
| High | Accessibility, jank, theme breakage, or cross-browser correctness |
| Medium | Mobile/responsive issues, overflow risks, structural concerns |
| Low | Naming, formatting, shorthand preferences |
